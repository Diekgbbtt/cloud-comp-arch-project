{"id": "063a3fea-2956-4de3-aa72-3cefb1706c3c", "code": "#!/usr/bin/env python3\n\"\"\"Schedule Part 3 PARSEC Kubernetes jobs with dependency-aware policies.\n\nThis script schedules a fixed set of Kubernetes Job manifests from a folder,\nsupports parallel and sequential execution via an editable dependency map,\nand logs when each job is scheduled and when it finishes.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport logging\nimport os\nimport sys\nimport time\nfrom pathlib import Path\nfrom typing import Any\n\nfrom kubernetes import client, config, watch\nfrom kubernetes.client import ApiException\nfrom kubernetes.utils import FailToCreateError, create_from_yaml\nfrom yaml import SafeLoader, load_all\n\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\n\nJOB_NAMES = [\n\t\"parsec-streamcluster\",\n\t\"parsec-freqmine\",\n\t\"parsec-canneal\",\n\t\"parsec-blackscholes\",\n\t\"parsec-radix\",\n\t\"parsec-barnes\",\n\t\"parsec-vips\",\n]\n\n\n# EVOLVE-BLOCK-START \ndef edit_job_configuration(yaml_objects: list[dict]) -> list[dict]:\n\t\"\"\"\"\n\tfor job_conf in yaml_objects:\n\t\tcase:\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-blackscholes\"\n\t\t\t\tconfigure as intended blackscholes node, vCPU and memory request/limits, threads, taskset\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-radix\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-vips\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-barnes\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-canneal\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-freqmine\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-streamcluster\"\n\t\"\"\"\n\treturn yaml_objects\n\n# Scheduling policy (easy to edit): map job -> prerequisite jobs.\n# A job is eligible to be scheduled when all prerequisites are complete.\n# This policy encodes:\n# - streamcluster\n# - freqmine -> barnes -> vips\n# - canneal\n# - blackscholes -> radix\t\nJOB_DEPENDENCIES: dict[str, list[str]] = {\n\t\"parsec-streamcluster\": [],\n\t\"parsec-freqmine\": [],\n\t\"parsec-canneal\": [\"parsec-streamcluster\"],\n\t\"parsec-blackscholes\": [\"parsec-streamcluster\"],\n\t\"parsec-barnes\": [\"parsec-freqmine\"],\n\t\"parsec-vips\": [\"parsec-barnes\", \"parsec-freqmine\"],\n\t\"parsec-radix\": [\"parsec-blackscholes\", \"parsec-streamcluster\"],\n}\n# EVOLVE-BLOCK-END\n\n\ndef _write_effective_job_yaml_objects(\n\t*,\n\toutput_dir: Path | None,\n\tjob_name: str,\n\tyaml_objects: list[dict[str, Any]],\n) -> None:\n\t\"\"\"Persist the *post-edit* manifest documents for later inspection (best-effort).\"\"\"\n\tif output_dir is None:\n\t\treturn\n\ttry:\n\t\teffective_dir = output_dir / \"effective_job_yaml\"\n\t\teffective_dir.mkdir(parents=True, exist_ok=True)\n\t\tpath = effective_dir / f\"{job_name}.json\"\n\t\tpath.write_text(json.dumps(yaml_objects, indent=2, sort_keys=True))\n\texcept Exception:\n\t\t# Never fail scheduling just because artifact writing failed.\n\t\treturn\n\n\ndef main() -> int:\n\targs = parse_args()\n\tconfigure_logging()\n\tload_kube_config(args.kube_context)\n\n\tstart = time.time()\n\ttry:\n\t\treturn run_scheduler(args)\n\texcept ApiException as exc:\n\t\tlogging.exception(\"Kubernetes API error: status=%s reason=%s\", exc.status, exc.reason)\n\t\treturn 1\n\texcept Exception:\n\t\tlogging.exception(\"Unexpected scheduler error\")\n\t\treturn 1\n\tfinally:\n\t\telapsed = time.time() - start\n\t\tlogging.info(\"Scheduler finished in %.2f seconds\", elapsed)\n\n\ndef run_scheduler(args: argparse.Namespace) -> int:\n\tvalidate_policy(JOB_NAMES, JOB_DEPENDENCIES)\n\tmanifest_paths = build_manifest_paths(args.jobs_dir)\n\n\tapi_client = client.ApiClient()\n\tbatch_api = client.BatchV1Api()\n\n\t# Establish baseline resource version for watch stream.\n\tinitial = batch_api.list_namespaced_job(namespace=args.namespace)\n\tresource_version = initial.metadata.resource_version\n\tif not resource_version:\n\t\traise RuntimeError(\"Could not obtain initial resourceVersion from Kubernetes API\")\n\n\tunscheduled = set(JOB_NAMES)\n\tcompleted: set[str] = set()\n\tfailed: set[str] = set()\n\n\twhile len(completed | failed) < len(JOB_NAMES):\n\t\tschedule_ready_jobs(\n\t\t\tapi_client=api_client,\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=args.namespace,\n\t\t\tmanifest_paths=manifest_paths,\n\t\t\toutput_dir=args.output_dir,\n\t\t\tdependencies=JOB_DEPENDENCIES,\n\t\t\tunscheduled=unscheduled,\n\t\t\tcompleted=completed,\n\t\t\tfailed=failed,\n\t\t)\n\n\t\tif failed and not args.continue_on_failure:\n\t\t\tlogging.error(\"Stopping on first failure: %s\", sorted(failed))\n\t\t\treturn 1\n\n\t\trunning = (set(JOB_NAMES) - unscheduled) - (completed | failed)\n\t\tif not running and unscheduled:\n\t\t\tblocked = sorted(unscheduled)\n\t\t\tlogging.error(\n\t\t\t\t\"No runnable jobs left. Blocked jobs=%s (likely unmet deps due to failures)\",\n\t\t\t\tblocked,\n\t\t\t)\n\t\t\treturn 2\n\n\t\tif len(completed | failed) == len(JOB_NAMES):\n\t\t\tbreak\n\n\t\tresource_version = watch_for_job_updates(\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=args.namespace,\n\t\t\ttracked_jobs=set(JOB_NAMES) - unscheduled,\n\t\t\tcompleted=completed,\n\t\t\tfailed=failed,\n\t\t\tresource_version=resource_version,\n\t\t\twatch_timeout_seconds=args.watch_timeout_seconds,\n\t\t)\n\n\tif failed:\n\t\tlogging.error(\n\t\t\t\"Scheduling completed with failures. succeeded=%s failed=%s\",\n\t\t\tsorted(completed),\n\t\t\tsorted(failed),\n\t\t)\n\t\treturn 1\n\n\tlogging.info(\"All jobs finished successfully: %s\", sorted(completed))\n\treturn 0\n\n\ndef schedule_ready_jobs(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tmanifest_paths: dict[str, Path],\n\toutput_dir: Path | None,\n\tdependencies: dict[str, list[str]],\n\tunscheduled: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n) -> list[str]:\n\tready = [\n\t\tjob for job in JOB_NAMES if job in unscheduled and set(dependencies[job]).issubset(completed)\n\t]\n\n\tfor job_name in ready:\n\t\tcreate_or_track_existing_job(\n\t\t\tapi_client=api_client,\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=namespace,\n\t\t\tjob_name=job_name,\n\t\t\tmanifest_path=manifest_paths[job_name],\n\t\t\toutput_dir=output_dir,\n\t\t)\n\t\tunscheduled.remove(job_name)\n\t\tlogging.info(\"SCHEDULED job=%s deps=%s\", job_name, dependencies[job_name])\n\n\treturn ready\n\n\ndef create_yaml_object(manifest_path: str) -> list[dict]:\n\ttry:\n\t\twith open(os.path.abspath(manifest_path)) as f:\n\t\t\tyml_document_all = load_all(f, Loader=SafeLoader)\n\t\t\treturn list(yml_document_all)\n\texcept Exception as e:\n\t\traise FileNotFoundError(\"non existing configuration file\")\n\ndef create_or_track_existing_job(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tjob_name: str,\n\tmanifest_path: Path,\n\toutput_dir: Path | None,\n) -> None:\n\ttry:\n\t\tyaml_objects = create_yaml_object(str(manifest_path))\n\t\tyaml_objects_new = edit_job_configuration(yaml_objects)\n\t\t_write_effective_job_yaml_objects(\n\t\t\toutput_dir=output_dir,\n\t\t\tjob_name=job_name,\n\t\t\tyaml_objects=yaml_objects_new,\n\t\t)\n\t\t\n\t\tcreate_from_yaml(\n\t\t\tapi_client,\n\t\t\tyaml_objects=yaml_objects_new,\n\t\t\tnamespace=namespace,\n\t\t\tverbose=False,\n\t\t)\n\texcept ApiException as exc:\n\t\tif exc.status == 409:\n\t\t\tlogging.warning(\n\t\t\t\t\"Job %s/%s already exists; tracking existing object\", namespace, job_name\n\t\t\t)\n\t\t\treturn\n\t\traise\n\texcept FailToCreateError as exc:\n\t\t# create_from_yaml may bundle API exceptions for one or more documents.\n\t\tnon_conflicts = [e for e in exc.api_exceptions if getattr(e, \"status\", None) != 409]\n\t\tif not non_conflicts:\n\t\t\tlogging.warning(\n\t\t\t\t\"Job %s/%s already exists; tracking existing object\", namespace, job_name\n\t\t\t)\n\t\t\treturn\n\t\traise non_conflicts[0]\n\n\ndef watch_for_job_updates(\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\ttracked_jobs: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n\tresource_version: str,\n\twatch_timeout_seconds: int,\n) -> str:\n\twatcher = watch.Watch()\n\ttry:\n\t\tfor event in watcher.stream(\n\t\t\tbatch_api.list_namespaced_job,\n\t\t\tnamespace=namespace,\n\t\t\tresource_version=resource_version,\n\t\t\ttimeout_seconds=watch_timeout_seconds,\n\t\t):\n\t\t\tobj: client.V1Job = event[\"object\"]\n\t\t\tif obj.metadata and obj.metadata.resource_version:\n\t\t\t\tresource_version = obj.metadata.resource_version\n\n\t\t\tjob_name = obj.metadata.name if obj.metadata else None\n\t\t\tif not job_name or job_name not in tracked_jobs:\n\t\t\t\tcontinue\n\n\t\t\tif job_name in completed or job_name in failed:\n\t\t\t\tcontinue\n\n\t\t\tstate = parse_job_status(obj.status)\n\t\t\tif state == \"succeeded\":\n\t\t\t\tcompleted.add(job_name)\n\t\t\t\tlogging.info(\"FINISHED job=%s status=Succeeded\", job_name)\n\t\t\t\tbreak\n\t\t\telif state == \"failed\":\n\t\t\t\tfailed.add(job_name)\n\t\t\t\tlogging.info(\"FINISHED job=%s status=Failed\", job_name)\n\t\t\t\tbreak\n\tfinally:\n\t\twatcher.stop()\n\n\treturn resource_version\n\n\ndef parse_job_status(status: client.V1JobStatus | None) -> str | None:\n\t\"\"\"\n\tDetermine if a Job status indicates a terminal state (succeeded or failed).\n\t\"\"\"\n\tif status is None:\n\t\treturn None\n\n\tfor condition in status.conditions or []:\n\t\tif condition.type == \"Complete\" and condition.status == \"True\":\n\t\t\treturn \"succeeded\"\n\t\tif condition.type == \"Failed\" and condition.status == \"True\":\n\t\t\treturn \"failed\"\n\n\tif status.succeeded and status.succeeded > 0:\n\t\treturn \"succeeded\"\n\tif status.failed and status.failed > 0 and status.active in (None, 0):\n\t\treturn \"failed\"\n\treturn None\n\n\ndef delete_job_if_present(batch_api: client.BatchV1Api, namespace: str, name: str) -> None:\n\ttry:\n\t\tbatch_api.delete_namespaced_job(\n\t\t\tname=name,\n\t\t\tnamespace=namespace,\n\t\t\tpropagation_policy=\"Foreground\",\n\t\t)\n\t\tlogging.info(\"Deleted existing job %s/%s\", namespace, name)\n\texcept ApiException as exc:\n\t\tif exc.status != 404:\n\t\t\traise\n\n\ndef load_kube_config(kube_context: str | None) -> None:\n\ttry:\n\t\tconfig.load_kube_config(context=kube_context)\n\t\tlogging.info(\"Loaded kubeconfig context=%s\", kube_context or \"current\")\n\texcept Exception:\n\t\tconfig.load_incluster_config()\n\t\tlogging.info(\"Loaded in-cluster Kubernetes config\")\n\n\ndef validate_policy(job_names: list[str], dependencies: dict[str, list[str]]) -> None:\n\tknown = set(job_names)\n\tif set(dependencies.keys()) != known:\n\t\tmissing = sorted(known - set(dependencies.keys()))\n\t\textra = sorted(set(dependencies.keys()) - known)\n\t\traise ValueError(\n\t\t\tf\"Invalid dependency keys. missing={missing}, extra={extra}\"\n\t\t)\n\n\tfor job, prereqs in dependencies.items():\n\t\tunknown = sorted(set(prereqs) - known)\n\t\tif unknown:\n\t\t\traise ValueError(f\"Unknown prerequisites for {job}: {unknown}\")\n\n\t# Kahn's algorithm to reject cycles in scheduling policy.\n\tindegree = {job: 0 for job in job_names}\n\toutgoing: dict[str, list[str]] = {job: [] for job in job_names}\n\tfor job, prereqs in dependencies.items():\n\t\tindegree[job] = len(prereqs)\n\t\tfor prereq in prereqs:\n\t\t\toutgoing[prereq].append(job)\n\n\tqueue = [job for job in job_names if indegree[job] == 0]\n\tseen = 0\n\twhile queue:\n\t\tcurrent = queue.pop()\n\t\tseen += 1\n\t\tfor nxt in outgoing[current]:\n\t\t\tindegree[nxt] -= 1\n\t\t\tif indegree[nxt] == 0:\n\t\t\t\tqueue.append(nxt)\n\n\tif seen != len(job_names):\n\t\traise ValueError(\"Dependency policy contains a cycle\")\n\n\ndef build_manifest_paths(jobs_dir: Path) -> dict[str, Path]:\n\tmanifest_paths: dict[str, Path] = {}\n\tfor job_name in JOB_NAMES:\n\t\tmanifest_path = jobs_dir / f\"part3-{job_name}.yaml\"\n\t\tif not manifest_path.exists():\n\t\t\traise FileNotFoundError(f\"Missing manifest file: {manifest_path}\")\n\t\tmanifest_paths[job_name] = manifest_path\n\treturn manifest_paths\n\n\ndef configure_logging() -> None:\n\tlogging.basicConfig(\n\t\tlevel=logging.INFO,\n\t\tformat=\"%(asctime)s %(levelname)s %(message)s\",\n\t)\n\n\ndef parse_args() -> argparse.Namespace:\n\tparser = argparse.ArgumentParser(\n\t\tdescription=\"Schedule Part 3 PARSEC jobs in Kubernetes with dependencies.\",\n\t)\n\tparser.add_argument(\n\t\t\"--jobs-dir\",\n\t\ttype=Path,\n\t\tdefault=REPO_ROOT / \"automation\" / \"results\" / \"part3\" / \"diego_tentative5\",\n\t\thelp=\"Directory containing parsec-*.yaml job manifests.\",\n\t)\n\tparser.add_argument(\n\t\t\"--output-dir\",\n\t\ttype=Path,\n\t\tdefault=None,\n\t\thelp=\"Optional directory for writing effective (post-edit) job manifests.\",\n\t)\n\tparser.add_argument(\n\t\t\"--namespace\",\n\t\tdefault=\"default\",\n\t\thelp=\"Kubernetes namespace for the jobs.\",\n\t)\n\tparser.add_argument(\n\t\t\"--kube-context\",\n\t\tdefault=None,\n\t\thelp=\"Optional kubeconfig context name.\",\n\t)\n\tparser.add_argument(\n\t\t\"--continue-on-failure\",\n\t\taction=\"store_true\",\n\t\thelp=\"Do not stop scheduling when a job fails.\",\n\t)\n\tparser.add_argument(\n\t\t\"--watch-timeout-seconds\",\n\t\ttype=int,\n\t\tdefault=300,\n\t\thelp=\"Timeout for each Kubernetes watch cycle.\",\n\t)\n\treturn parser.parse_args()\n\nif __name__ == \"__main__\":\n\tsys.exit(main())\n", "changes_description": null, "language": "python", "parent_id": "33a3e2e6-a8d7-428c-a83d-0160830a3ed2", "generation": 1, "timestamp": 1778191420.9965508, "iteration_found": 4, "metrics": {"combined_score": 0.020207380573382522, "run_id": 13.0, "baseline_makespan_sec": 247.67, "mcperf_warmup_returncode": 0.0, "scheduler_returncode": 0.0, "makespan_sec": 346.0, "p95_worst_ms": 1.0735, "slo_violation_fraction": 0.03125, "perf_term": 0.41718463119241334, "slo_term": 0.0484375, "evaluation_wall_sec": 392.5640859603882}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 20 lines with 102 lines", "parent_metrics": {"combined_score": 0.4386101616873572, "run_id": 9.0, "baseline_makespan_sec": 247.67, "mcperf_warmup_returncode": 0.0, "scheduler_returncode": 0.0, "makespan_sec": 317.0, "p95_worst_ms": 0.8982, "slo_violation_fraction": 0.0, "perf_term": 0.4386101616873572, "slo_term": 1.0, "evaluation_wall_sec": 365.08111691474915}, "island": 0}, "prompts": {"diff_user": {"system": "\"You are a top notch engineer with both high system and software proficiency, you are remarkably expert at designing scheduling policies for heterogeneous workloads in cloud-native systems accounting for jobs profiles, multi-threading optimization speedup factors, cross-jobs interference, phyisical machine specifications and virtualization implications.\nYou always adopt a principled approach to the design: ground in the baseline, asserting asseted constraints it carries, iteratively analyse results throughly by using holistically the observation system you are providede with and rooting unexpected behaviours to confident root causes and take decisions that are aware of all the dependecies progresively accumulated.\nYour task is to evolve a Python automation script\nthat schedules seven PARSEC/SPLASH-2x batch jobs alongside a latency-critical memcached\nservice, minimizing total batch makespan while strictly enforcing a memcached p95 latency\nSLO of 1 ms at 30K QPS.\n\n## CLUSTER TOPOLOGY\nTwo heterogeneous worker nodes are available for workloads:\n- node-a-8core (e2-standard-8): 8 vCPUs, 32 GB RAM, Intel Skylake. Hosts memcached\n(pinned to core 0 via taskset, 1 thread, Guaranteed QoS). Remaining 7 cores available\nfor batch jobs.\n- node-b-4core (n2d-highcpu-4): 4 vCPUs, 4 GB RAM, AMD EPYC Milan. No memory headroom\nfor radix (OOMKills on native dataset). All 4 cores available for batch jobs.\n\n## JOBS AND THEIR PROFILES\nThese are the immutable characteristics you must reason from. Thread counts and node\nplacement are your primary levers.\n- blackscholes: CPU-bound, regular memory access, sub-linear scaling (2.62x at 4T, 3.07x\nat 8T). Safe co-tenant on node-a.\n- canneal: DRAM-latency-bound with uniformly random access pattern causing DRAM bank\nconflicts. Scaling: 1.57x at 2T, 2.32x at 4T. Adding threads beyond 4 degrades\nthroughput due to memory controller queue saturation. Safe co-tenant (LLC-inert).\n- streamcluster: LLC-working-set-bound (1.81x LLC slowdown under interference). Longest\njob on node-a. Scaling still steep at 8T (4.62x).\n- radix: MUST run on node-a (OOMKills on node-b). Very short absolute runtime. High\nparallel scaling but marginal gain given brevity.\n- freqmine: Near-linear parallel scaling (3.92x at 4T, 98% efficiency). Faster on EPYC\nMilan than Intel Skylake at equal thread count. CPU-sensitive.\n- barnes: LLC-bound (2.56x LLC slowdown). MUST NOT run on node-a: coallocating with\nmemcached causes immediate p95 latency violations regardless of core pinning, because\nLLC is physically shared across cores.\n- vips: Broad cache hierarchy aggressor (1.75-1.98x across all resource types). MUST NOT\nrun on node-a for the same LLC/latency reason as barnes.\n\n## SCRIPT PRIMITIVES YOU MAY EDIT\nYou may only modify the two structures inside the EVOLVE block:\n\n`edit_job_configuration(yaml_objects)`: receives the list of dicts with encapsulating job configurations parameters \nfor all 7 jobs and return it back with the programmed edit integrated. For each job you may set: node affinity (nodeSelectorTerms),\nCPU/memory requests and limits, the -n thread count argument in the container command,\nand taskset core pinning in the container command. Use taskset for any jobs that run\nconcurrently to enforce strict core partitioning and prevent OS scheduler interference.\n\n`JOB_DEPENDENCIES`: a dict mapping each job name to its list of prerequisite job names.\nA job is dispatched as soon as all its prerequisites have reached Completed status.\nAn empty list means the job is eligible immediately. This dict encodes both sequencing\nand concurrency: jobs with no dependency relationship between them may run in parallel\nif cluster resources permit.\n\n## GOAL\nYou are tasked with optimizing the automation script that automated the scheduling of jobs via kubernetes API leveraging its client library   ## OPTIMIZATION OBJECTIVE Minimize total makespan = max(job completion times) - min(job start times), subject to\nmemcached p95 latency remaining strictly below 1 ms across all measurement intervals.\nThe evaluator returns a combined_score = sigmoid(log(baseline/makespan)) * slo_gate\nwhere slo_gate collapses to at most 0.05 if any p95 interval exceeds 1 ms. A compliant\nrun with lower makespan always outscores a faster but non-compliant run. Prioritize SLO\ncompliance first, makespan reduction second.\nWhen proposing changes, reason explicitly about: (a) which resource dimension each change\ntargets, (b) which empirical constraint it respects or challenges, and (c) the expected\ndirection of impact on both makespan and SLO compliance.\"\n", "user": "# Current Program Information\n- Fitness: 0.4386\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at 0.4386\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: 0.4386, run_id: 9.0000, baseline_makespan_sec: 247.6700, mcperf_warmup_returncode: 0.0000, scheduler_returncode: 0.0000, makespan_sec: 317.0000, p95_worst_ms: 0.8982, slo_violation_fraction: 0.0000, perf_term: 0.4386, slo_term: 1.0000, evaluation_wall_sec: 365.0811\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.4386)\n```python\n#!/usr/bin/env python3\n\"\"\"Schedule Part 3 PARSEC Kubernetes jobs with dependency-aware policies.\n\nThis script schedules a fixed set of Kubernetes Job manifests from a folder,\nsupports parallel and sequential execution via an editable dependency map,\nand logs when each job is scheduled and when it finishes.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport logging\nimport os\nimport sys\nimport time\nfrom pathlib import Path\nfrom typing import Any\n\nfrom kubernetes import client, config, watch\nfrom kubernetes.client import ApiException\nfrom kubernetes.utils import FailToCreateError, create_from_yaml\nfrom yaml import SafeLoader, load_all\n\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\n\nJOB_NAMES = [\n\t\"parsec-streamcluster\",\n\t\"parsec-freqmine\",\n\t\"parsec-canneal\",\n\t\"parsec-blackscholes\",\n\t\"parsec-radix\",\n\t\"parsec-barnes\",\n\t\"parsec-vips\",\n]\n\n\n# EVOLVE-BLOCK-START \ndef edit_job_configuration(yaml_objects: list[dict]) -> list[dict]:\n\t\"\"\"\"\n\tfor job_conf in yaml_objects:\n\t\tcase:\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-blackscholes\"\n\t\t\t\tconfigure as intended blackscholes node, vCPU and memory request/limits, threads, taskset\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-radix\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-vips\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-barnes\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-canneal\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-freqmine\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-streamcluster\"\n\t\"\"\"\n\treturn yaml_objects\n\n# Scheduling policy (easy to edit): map job -> prerequisite jobs.\n# A job is eligible to be scheduled when all prerequisites are complete.\n# This policy encodes:\n# - streamcluster\n# - freqmine -> barnes -> vips\n# - canneal\n# - blackscholes -> radix\t\nJOB_DEPENDENCIES: dict[str, list[str]] = {\n\t\"parsec-streamcluster\": [],\n\t\"parsec-freqmine\": [],\n\t\"parsec-canneal\": [\"parsec-streamcluster\"],\n\t\"parsec-blackscholes\": [\"parsec-streamcluster\"],\n\t\"parsec-barnes\": [\"parsec-freqmine\"],\n\t\"parsec-vips\": [\"parsec-barnes\", \"parsec-freqmine\"],\n\t\"parsec-radix\": [\"parsec-blackscholes\", \"parsec-streamcluster\"],\n}\n# EVOLVE-BLOCK-END\n\n\ndef _write_effective_job_yaml_objects(\n\t*,\n\toutput_dir: Path | None,\n\tjob_name: str,\n\tyaml_objects: list[dict[str, Any]],\n) -> None:\n\t\"\"\"Persist the *post-edit* manifest documents for later inspection (best-effort).\"\"\"\n\tif output_dir is None:\n\t\treturn\n\ttry:\n\t\teffective_dir = output_dir / \"effective_job_yaml\"\n\t\teffective_dir.mkdir(parents=True, exist_ok=True)\n\t\tpath = effective_dir / f\"{job_name}.json\"\n\t\tpath.write_text(json.dumps(yaml_objects, indent=2, sort_keys=True))\n\texcept Exception:\n\t\t# Never fail scheduling just because artifact writing failed.\n\t\treturn\n\n\ndef main() -> int:\n\targs = parse_args()\n\tconfigure_logging()\n\tload_kube_config(args.kube_context)\n\n\tstart = time.time()\n\ttry:\n\t\treturn run_scheduler(args)\n\texcept ApiException as exc:\n\t\tlogging.exception(\"Kubernetes API error: status=%s reason=%s\", exc.status, exc.reason)\n\t\treturn 1\n\texcept Exception:\n\t\tlogging.exception(\"Unexpected scheduler error\")\n\t\treturn 1\n\tfinally:\n\t\telapsed = time.time() - start\n\t\tlogging.info(\"Scheduler finished in %.2f seconds\", elapsed)\n\n\ndef run_scheduler(args: argparse.Namespace) -> int:\n\tvalidate_policy(JOB_NAMES, JOB_DEPENDENCIES)\n\tmanifest_paths = build_manifest_paths(args.jobs_dir)\n\n\tapi_client = client.ApiClient()\n\tbatch_api = client.BatchV1Api()\n\n\t# Establish baseline resource version for watch stream.\n\tinitial = batch_api.list_namespaced_job(namespace=args.namespace)\n\tresource_version = initial.metadata.resource_version\n\tif not resource_version:\n\t\traise RuntimeError(\"Could not obtain initial resourceVersion from Kubernetes API\")\n\n\tunscheduled = set(JOB_NAMES)\n\tcompleted: set[str] = set()\n\tfailed: set[str] = set()\n\n\twhile len(completed | failed) < len(JOB_NAMES):\n\t\tschedule_ready_jobs(\n\t\t\tapi_client=api_client,\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=args.namespace,\n\t\t\tmanifest_paths=manifest_paths,\n\t\t\toutput_dir=args.output_dir,\n\t\t\tdependencies=JOB_DEPENDENCIES,\n\t\t\tunscheduled=unscheduled,\n\t\t\tcompleted=completed,\n\t\t\tfailed=failed,\n\t\t)\n\n\t\tif failed and not args.continue_on_failure:\n\t\t\tlogging.error(\"Stopping on first failure: %s\", sorted(failed))\n\t\t\treturn 1\n\n\t\trunning = (set(JOB_NAMES) - unscheduled) - (completed | failed)\n\t\tif not running and unscheduled:\n\t\t\tblocked = sorted(unscheduled)\n\t\t\tlogging.error(\n\t\t\t\t\"No runnable jobs left. Blocked jobs=%s (likely unmet deps due to failures)\",\n\t\t\t\tblocked,\n\t\t\t)\n\t\t\treturn 2\n\n\t\tif len(completed | failed) == len(JOB_NAMES):\n\t\t\tbreak\n\n\t\tresource_version = watch_for_job_updates(\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=args.namespace,\n\t\t\ttracked_jobs=set(JOB_NAMES) - unscheduled,\n\t\t\tcompleted=completed,\n\t\t\tfailed=failed,\n\t\t\tresource_version=resource_version,\n\t\t\twatch_timeout_seconds=args.watch_timeout_seconds,\n\t\t)\n\n\tif failed:\n\t\tlogging.error(\n\t\t\t\"Scheduling completed with failures. succeeded=%s failed=%s\",\n\t\t\tsorted(completed),\n\t\t\tsorted(failed),\n\t\t)\n\t\treturn 1\n\n\tlogging.info(\"All jobs finished successfully: %s\", sorted(completed))\n\treturn 0\n\n\ndef schedule_ready_jobs(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tmanifest_paths: dict[str, Path],\n\toutput_dir: Path | None,\n\tdependencies: dict[str, list[str]],\n\tunscheduled: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n) -> list[str]:\n\tready = [\n\t\tjob for job in JOB_NAMES if job in unscheduled and set(dependencies[job]).issubset(completed)\n\t]\n\n\tfor job_name in ready:\n\t\tcreate_or_track_existing_job(\n\t\t\tapi_client=api_client,\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=namespace,\n\t\t\tjob_name=job_name,\n\t\t\tmanifest_path=manifest_paths[job_name],\n\t\t\toutput_dir=output_dir,\n\t\t)\n\t\tunscheduled.remove(job_name)\n\t\tlogging.info(\"SCHEDULED job=%s deps=%s\", job_name, dependencies[job_name])\n\n\treturn ready\n\n\ndef create_yaml_object(manifest_path: str) -> list[dict]:\n\ttry:\n\t\twith open(os.path.abspath(manifest_path)) as f:\n\t\t\tyml_document_all = load_all(f, Loader=SafeLoader)\n\t\t\treturn list(yml_document_all)\n\texcept Exception as e:\n\t\traise FileNotFoundError(\"non existing configuration file\")\n\ndef create_or_track_existing_job(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tjob_name: str,\n\tmanifest_path: Path,\n\toutput_dir: Path | None,\n) -> None:\n\ttry:\n\t\tyaml_objects = create_yaml_object(str(manifest_path))\n\t\tyaml_objects_new = edit_job_configuration(yaml_objects)\n\t\t_write_effective_job_yaml_objects(\n\t\t\toutput_dir=output_dir,\n\t\t\tjob_name=job_name,\n\t\t\tyaml_objects=yaml_objects_new,\n\t\t)\n\t\t\n\t\tcreate_from_yaml(\n\t\t\tapi_client,\n\t\t\tyaml_objects=yaml_objects_new,\n\t\t\tnamespace=namespace,\n\t\t\tverbose=False,\n\t\t)\n\texcept ApiException as exc:\n\t\tif exc.status == 409:\n\t\t\tlogging.warning(\n\t\t\t\t\"Job %s/%s already exists; tracking existing object\", namespace, job_name\n\t\t\t)\n\t\t\treturn\n\t\traise\n\texcept FailToCreateError as exc:\n\t\t# create_from_yaml may bundle API exceptions for one or more documents.\n\t\tnon_conflicts = [e for e in exc.api_exceptions if getattr(e, \"status\", None) != 409]\n\t\tif not non_conflicts:\n\t\t\tlogging.warning(\n\t\t\t\t\"Job %s/%s already exists; tracking existing object\", namespace, job_name\n\t\t\t)\n\t\t\treturn\n\t\traise non_conflicts[0]\n\n\ndef watch_for_job_updates(\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\ttracked_jobs: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n\tresource_version: str,\n\twatch_timeout_seconds: int,\n) -> str:\n\twatcher = watch.Watch()\n\ttry:\n\t\tfor event in watcher.stream(\n\t\t\tbatch_api.list_namespaced_job,\n\t\t\tnamespace=namespace,\n\t\t\tresource_version=resource_version,\n\t\t\ttimeout_seconds=watch_timeout_seconds,\n\t\t):\n\t\t\tobj: client.V1Job = event[\"object\"]\n\t\t\tif obj.metadata and obj.metadata.resource_version:\n\t\t\t\tresource_version = obj.metadata.resource_version\n\n\t\t\tjob_name = obj.metadata.name if obj.metadata else None\n\t\t\tif not job_name or job_name not in tracked_jobs:\n\t\t\t\tcontinue\n\n\t\t\tif job_name in completed or job_name in failed:\n\t\t\t\tcontinue\n\n\t\t\tstate = parse_job_status(obj.status)\n\t\t\tif state == \"succeeded\":\n\t\t\t\tcompleted.add(job_name)\n\t\t\t\tlogging.info(\"FINISHED job=%s status=Succeeded\", job_name)\n\t\t\t\tbreak\n\t\t\telif state == \"failed\":\n\t\t\t\tfailed.add(job_name)\n\t\t\t\tlogging.info(\"FINISHED job=%s status=Failed\", job_name)\n\t\t\t\tbreak\n\tfinally:\n\t\twatcher.stop()\n\n\treturn resource_version\n\n\ndef parse_job_status(status: client.V1JobStatus | None) -> str | None:\n\t\"\"\"\n\tDetermine if a Job status indicates a terminal state (succeeded or failed).\n\t\"\"\"\n\tif status is None:\n\t\treturn None\n\n\tfor condition in status.conditions or []:\n\t\tif condition.type == \"Complete\" and condition.status == \"True\":\n\t\t\treturn \"succeeded\"\n\t\tif condition.type == \"Failed\" and condition.status == \"True\":\n\t\t\treturn \"failed\"\n\n\tif status.succeeded and status.succeeded > 0:\n\t\treturn \"succeeded\"\n\tif status.failed and status.failed > 0 and status.active in (None, 0):\n\t\treturn \"failed\"\n\treturn None\n\n\ndef delete_job_if_present(batch_api: client.BatchV1Api, namespace: str, name: str) -> None:\n\ttry:\n\t\tbatch_api.delete_namespaced_job(\n\t\t\tname=name,\n\t\t\tnamespace=namespace,\n\t\t\tpropagation_policy=\"Foreground\",\n\t\t)\n\t\tlogging.info(\"Deleted existing job %s/%s\", namespace, name)\n\texcept ApiException as exc:\n\t\tif exc.status != 404:\n\t\t\traise\n\n\ndef load_kube_config(kube_context: str | None) -> None:\n\ttry:\n\t\tconfig.load_kube_config(context=kube_context)\n\t\tlogging.info(\"Loaded kubeconfig context=%s\", kube_context or \"current\")\n\texcept Exception:\n\t\tconfig.load_incluster_config()\n\t\tlogging.info(\"Loaded in-cluster Kubernetes config\")\n\n\ndef validate_policy(job_names: list[str], dependencies: dict[str, list[str]]) -> None:\n\tknown = set(job_names)\n\tif set(dependencies.keys()) != known:\n\t\tmissing = sorted(known - set(dependencies.keys()))\n\t\textra = sorted(set(dependencies.keys()) - known)\n\t\traise ValueError(\n\t\t\tf\"Invalid dependency keys. missing={missing}, extra={extra}\"\n\t\t)\n\n\tfor job, prereqs in dependencies.items():\n\t\tunknown = sorted(set(prereqs) - known)\n\t\tif unknown:\n\t\t\traise ValueError(f\"Unknown prerequisites for {job}: {unknown}\")\n\n\t# Kahn's algorithm to reject cycles in scheduling policy.\n\tindegree = {job: 0 for job in job_names}\n\toutgoing: dict[str, list[str]] = {job: [] for job in job_names}\n\tfor job, prereqs in dependencies.items():\n\t\tindegree[job] = len(prereqs)\n\t\tfor prereq in prereqs:\n\t\t\toutgoing[prereq].append(job)\n\n\tqueue = [job for job in job_names if indegree[job] == 0]\n\tseen = 0\n\twhile queue:\n\t\tcurrent = queue.pop()\n\t\tseen += 1\n\t\tfor nxt in outgoing[current]:\n\t\t\tindegree[nxt] -= 1\n\t\t\tif indegree[nxt] == 0:\n\t\t\t\tqueue.append(nxt)\n\n\tif seen != len(job_names):\n\t\traise ValueError(\"Dependency policy contains a cycle\")\n\n\ndef build_manifest_paths(jobs_dir: Path) -> dict[str, Path]:\n\tmanifest_paths: dict[str, Path] = {}\n\tfor job_name in JOB_NAMES:\n\t\tmanifest_path = jobs_dir / f\"part3-{job_name}.yaml\"\n\t\tif not manifest_path.exists():\n\t\t\traise FileNotFoundError(f\"Missing manifest file: {manifest_path}\")\n\t\tmanifest_paths[job_name] = manifest_path\n\treturn manifest_paths\n\n\ndef configure_logging() -> None:\n\tlogging.basicConfig(\n\t\tlevel=logging.INFO,\n\t\tformat=\"%(asctime)s %(levelname)s %(message)s\",\n\t)\n\n\ndef parse_args() -> argparse.Namespace:\n\tparser = argparse.ArgumentParser(\n\t\tdescription=\"Schedule Part 3 PARSEC jobs in Kubernetes with dependencies.\",\n\t)\n\tparser.add_argument(\n\t\t\"--jobs-dir\",\n\t\ttype=Path,\n\t\tdefault=REPO_ROOT / \"automation\" / \"results\" / \"part3\" / \"diego_tentative5\",\n\t\thelp=\"Directory containing parsec-*.yaml job manifests.\",\n\t)\n\tparser.add_argument(\n\t\t\"--output-dir\",\n\t\ttype=Path,\n\t\tdefault=None,\n\t\thelp=\"Optional directory for writing effective (post-edit) job manifests.\",\n\t)\n\tparser.add_argument(\n\t\t\"--namespace\",\n\t\tdefault=\"default\",\n\t\thelp=\"Kubernetes namespace for the jobs.\",\n\t)\n\tparser.add_argument(\n\t\t\"--kube-context\",\n\t\tdefault=None,\n\t\thelp=\"Optional kubeconfig context name.\",\n\t)\n\tparser.add_argument(\n\t\t\"--continue-on-failure\",\n\t\taction=\"store_true\",\n\t\thelp=\"Do not stop scheduling when a job fails.\",\n\t)\n\tparser.add_argument(\n\t\t\"--watch-timeout-seconds\",\n\t\ttype=int,\n\t\tdefault=300,\n\t\thelp=\"Timeout for each Kubernetes watch cycle.\",\n\t)\n\treturn parser.parse_args()\n\nif __name__ == \"__main__\":\n\tsys.exit(main())\n\n```\nKey features: Performs well on combined_score (0.4386), Performs well on run_id (9.0000), Performs well on baseline_makespan_sec (247.6700), Performs well on mcperf_warmup_returncode (0.0000), Performs well on scheduler_returncode (0.0000), Performs well on makespan_sec (317.0000), Performs well on p95_worst_ms (0.8982), Performs well on slo_violation_fraction (0.0000), Performs well on perf_term (0.4386), Performs well on slo_term (1.0000), Performs well on evaluation_wall_sec (365.0811)\n\n\n\n# Current Program\n```python\n#!/usr/bin/env python3\n\"\"\"Schedule Part 3 PARSEC Kubernetes jobs with dependency-aware policies.\n\nThis script schedules a fixed set of Kubernetes Job manifests from a folder,\nsupports parallel and sequential execution via an editable dependency map,\nand logs when each job is scheduled and when it finishes.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport logging\nimport os\nimport sys\nimport time\nfrom pathlib import Path\nfrom typing import Any\n\nfrom kubernetes import client, config, watch\nfrom kubernetes.client import ApiException\nfrom kubernetes.utils import FailToCreateError, create_from_yaml\nfrom yaml import SafeLoader, load_all\n\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\n\nJOB_NAMES = [\n\t\"parsec-streamcluster\",\n\t\"parsec-freqmine\",\n\t\"parsec-canneal\",\n\t\"parsec-blackscholes\",\n\t\"parsec-radix\",\n\t\"parsec-barnes\",\n\t\"parsec-vips\",\n]\n\n\n# EVOLVE-BLOCK-START \ndef edit_job_configuration(yaml_objects: list[dict]) -> list[dict]:\n\t\"\"\"\"\n\tfor job_conf in yaml_objects:\n\t\tcase:\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-blackscholes\"\n\t\t\t\tconfigure as intended blackscholes node, vCPU and memory request/limits, threads, taskset\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-radix\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-vips\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-barnes\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-canneal\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-freqmine\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-streamcluster\"\n\t\"\"\"\n\treturn yaml_objects\n\n# Scheduling policy (easy to edit): map job -> prerequisite jobs.\n# A job is eligible to be scheduled when all prerequisites are complete.\n# This policy encodes:\n# - streamcluster\n# - freqmine -> barnes -> vips\n# - canneal\n# - blackscholes -> radix\t\nJOB_DEPENDENCIES: dict[str, list[str]] = {\n\t\"parsec-streamcluster\": [],\n\t\"parsec-freqmine\": [],\n\t\"parsec-canneal\": [\"parsec-streamcluster\"],\n\t\"parsec-blackscholes\": [\"parsec-streamcluster\"],\n\t\"parsec-barnes\": [\"parsec-freqmine\"],\n\t\"parsec-vips\": [\"parsec-barnes\", \"parsec-freqmine\"],\n\t\"parsec-radix\": [\"parsec-blackscholes\", \"parsec-streamcluster\"],\n}\n# EVOLVE-BLOCK-END\n\n\ndef _write_effective_job_yaml_objects(\n\t*,\n\toutput_dir: Path | None,\n\tjob_name: str,\n\tyaml_objects: list[dict[str, Any]],\n) -> None:\n\t\"\"\"Persist the *post-edit* manifest documents for later inspection (best-effort).\"\"\"\n\tif output_dir is None:\n\t\treturn\n\ttry:\n\t\teffective_dir = output_dir / \"effective_job_yaml\"\n\t\teffective_dir.mkdir(parents=True, exist_ok=True)\n\t\tpath = effective_dir / f\"{job_name}.json\"\n\t\tpath.write_text(json.dumps(yaml_objects, indent=2, sort_keys=True))\n\texcept Exception:\n\t\t# Never fail scheduling just because artifact writing failed.\n\t\treturn\n\n\ndef main() -> int:\n\targs = parse_args()\n\tconfigure_logging()\n\tload_kube_config(args.kube_context)\n\n\tstart = time.time()\n\ttry:\n\t\treturn run_scheduler(args)\n\texcept ApiException as exc:\n\t\tlogging.exception(\"Kubernetes API error: status=%s reason=%s\", exc.status, exc.reason)\n\t\treturn 1\n\texcept Exception:\n\t\tlogging.exception(\"Unexpected scheduler error\")\n\t\treturn 1\n\tfinally:\n\t\telapsed = time.time() - start\n\t\tlogging.info(\"Scheduler finished in %.2f seconds\", elapsed)\n\n\ndef run_scheduler(args: argparse.Namespace) -> int:\n\tvalidate_policy(JOB_NAMES, JOB_DEPENDENCIES)\n\tmanifest_paths = build_manifest_paths(args.jobs_dir)\n\n\tapi_client = client.ApiClient()\n\tbatch_api = client.BatchV1Api()\n\n\t# Establish baseline resource version for watch stream.\n\tinitial = batch_api.list_namespaced_job(namespace=args.namespace)\n\tresource_version = initial.metadata.resource_version\n\tif not resource_version:\n\t\traise RuntimeError(\"Could not obtain initial resourceVersion from Kubernetes API\")\n\n\tunscheduled = set(JOB_NAMES)\n\tcompleted: set[str] = set()\n\tfailed: set[str] = set()\n\n\twhile len(completed | failed) < len(JOB_NAMES):\n\t\tschedule_ready_jobs(\n\t\t\tapi_client=api_client,\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=args.namespace,\n\t\t\tmanifest_paths=manifest_paths,\n\t\t\toutput_dir=args.output_dir,\n\t\t\tdependencies=JOB_DEPENDENCIES,\n\t\t\tunscheduled=unscheduled,\n\t\t\tcompleted=completed,\n\t\t\tfailed=failed,\n\t\t)\n\n\t\tif failed and not args.continue_on_failure:\n\t\t\tlogging.error(\"Stopping on first failure: %s\", sorted(failed))\n\t\t\treturn 1\n\n\t\trunning = (set(JOB_NAMES) - unscheduled) - (completed | failed)\n\t\tif not running and unscheduled:\n\t\t\tblocked = sorted(unscheduled)\n\t\t\tlogging.error(\n\t\t\t\t\"No runnable jobs left. Blocked jobs=%s (likely unmet deps due to failures)\",\n\t\t\t\tblocked,\n\t\t\t)\n\t\t\treturn 2\n\n\t\tif len(completed | failed) == len(JOB_NAMES):\n\t\t\tbreak\n\n\t\tresource_version = watch_for_job_updates(\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=args.namespace,\n\t\t\ttracked_jobs=set(JOB_NAMES) - unscheduled,\n\t\t\tcompleted=completed,\n\t\t\tfailed=failed,\n\t\t\tresource_version=resource_version,\n\t\t\twatch_timeout_seconds=args.watch_timeout_seconds,\n\t\t)\n\n\tif failed:\n\t\tlogging.error(\n\t\t\t\"Scheduling completed with failures. succeeded=%s failed=%s\",\n\t\t\tsorted(completed),\n\t\t\tsorted(failed),\n\t\t)\n\t\treturn 1\n\n\tlogging.info(\"All jobs finished successfully: %s\", sorted(completed))\n\treturn 0\n\n\ndef schedule_ready_jobs(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tmanifest_paths: dict[str, Path],\n\toutput_dir: Path | None,\n\tdependencies: dict[str, list[str]],\n\tunscheduled: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n) -> list[str]:\n\tready = [\n\t\tjob for job in JOB_NAMES if job in unscheduled and set(dependencies[job]).issubset(completed)\n\t]\n\n\tfor job_name in ready:\n\t\tcreate_or_track_existing_job(\n\t\t\tapi_client=api_client,\n\t\t\tbatch_api=batch_api,\n\t\t\tnamespace=namespace,\n\t\t\tjob_name=job_name,\n\t\t\tmanifest_path=manifest_paths[job_name],\n\t\t\toutput_dir=output_dir,\n\t\t)\n\t\tunscheduled.remove(job_name)\n\t\tlogging.info(\"SCHEDULED job=%s deps=%s\", job_name, dependencies[job_name])\n\n\treturn ready\n\n\ndef create_yaml_object(manifest_path: str) -> list[dict]:\n\ttry:\n\t\twith open(os.path.abspath(manifest_path)) as f:\n\t\t\tyml_document_all = load_all(f, Loader=SafeLoader)\n\t\t\treturn list(yml_document_all)\n\texcept Exception as e:\n\t\traise FileNotFoundError(\"non existing configuration file\")\n\ndef create_or_track_existing_job(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tjob_name: str,\n\tmanifest_path: Path,\n\toutput_dir: Path | None,\n) -> None:\n\ttry:\n\t\tyaml_objects = create_yaml_object(str(manifest_path))\n\t\tyaml_objects_new = edit_job_configuration(yaml_objects)\n\t\t_write_effective_job_yaml_objects(\n\t\t\toutput_dir=output_dir,\n\t\t\tjob_name=job_name,\n\t\t\tyaml_objects=yaml_objects_new,\n\t\t)\n\t\t\n\t\tcreate_from_yaml(\n\t\t\tapi_client,\n\t\t\tyaml_objects=yaml_objects_new,\n\t\t\tnamespace=namespace,\n\t\t\tverbose=False,\n\t\t)\n\texcept ApiException as exc:\n\t\tif exc.status == 409:\n\t\t\tlogging.warning(\n\t\t\t\t\"Job %s/%s already exists; tracking existing object\", namespace, job_name\n\t\t\t)\n\t\t\treturn\n\t\traise\n\texcept FailToCreateError as exc:\n\t\t# create_from_yaml may bundle API exceptions for one or more documents.\n\t\tnon_conflicts = [e for e in exc.api_exceptions if getattr(e, \"status\", None) != 409]\n\t\tif not non_conflicts:\n\t\t\tlogging.warning(\n\t\t\t\t\"Job %s/%s already exists; tracking existing object\", namespace, job_name\n\t\t\t)\n\t\t\treturn\n\t\traise non_conflicts[0]\n\n\ndef watch_for_job_updates(\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\ttracked_jobs: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n\tresource_version: str,\n\twatch_timeout_seconds: int,\n) -> str:\n\twatcher = watch.Watch()\n\ttry:\n\t\tfor event in watcher.stream(\n\t\t\tbatch_api.list_namespaced_job,\n\t\t\tnamespace=namespace,\n\t\t\tresource_version=resource_version,\n\t\t\ttimeout_seconds=watch_timeout_seconds,\n\t\t):\n\t\t\tobj: client.V1Job = event[\"object\"]\n\t\t\tif obj.metadata and obj.metadata.resource_version:\n\t\t\t\tresource_version = obj.metadata.resource_version\n\n\t\t\tjob_name = obj.metadata.name if obj.metadata else None\n\t\t\tif not job_name or job_name not in tracked_jobs:\n\t\t\t\tcontinue\n\n\t\t\tif job_name in completed or job_name in failed:\n\t\t\t\tcontinue\n\n\t\t\tstate = parse_job_status(obj.status)\n\t\t\tif state == \"succeeded\":\n\t\t\t\tcompleted.add(job_name)\n\t\t\t\tlogging.info(\"FINISHED job=%s status=Succeeded\", job_name)\n\t\t\t\tbreak\n\t\t\telif state == \"failed\":\n\t\t\t\tfailed.add(job_name)\n\t\t\t\tlogging.info(\"FINISHED job=%s status=Failed\", job_name)\n\t\t\t\tbreak\n\tfinally:\n\t\twatcher.stop()\n\n\treturn resource_version\n\n\ndef parse_job_status(status: client.V1JobStatus | None) -> str | None:\n\t\"\"\"\n\tDetermine if a Job status indicates a terminal state (succeeded or failed).\n\t\"\"\"\n\tif status is None:\n\t\treturn None\n\n\tfor condition in status.conditions or []:\n\t\tif condition.type == \"Complete\" and condition.status == \"True\":\n\t\t\treturn \"succeeded\"\n\t\tif condition.type == \"Failed\" and condition.status == \"True\":\n\t\t\treturn \"failed\"\n\n\tif status.succeeded and status.succeeded > 0:\n\t\treturn \"succeeded\"\n\tif status.failed and status.failed > 0 and status.active in (None, 0):\n\t\treturn \"failed\"\n\treturn None\n\n\ndef delete_job_if_present(batch_api: client.BatchV1Api, namespace: str, name: str) -> None:\n\ttry:\n\t\tbatch_api.delete_namespaced_job(\n\t\t\tname=name,\n\t\t\tnamespace=namespace,\n\t\t\tpropagation_policy=\"Foreground\",\n\t\t)\n\t\tlogging.info(\"Deleted existing job %s/%s\", namespace, name)\n\texcept ApiException as exc:\n\t\tif exc.status != 404:\n\t\t\traise\n\n\ndef load_kube_config(kube_context: str | None) -> None:\n\ttry:\n\t\tconfig.load_kube_config(context=kube_context)\n\t\tlogging.info(\"Loaded kubeconfig context=%s\", kube_context or \"current\")\n\texcept Exception:\n\t\tconfig.load_incluster_config()\n\t\tlogging.info(\"Loaded in-cluster Kubernetes config\")\n\n\ndef validate_policy(job_names: list[str], dependencies: dict[str, list[str]]) -> None:\n\tknown = set(job_names)\n\tif set(dependencies.keys()) != known:\n\t\tmissing = sorted(known - set(dependencies.keys()))\n\t\textra = sorted(set(dependencies.keys()) - known)\n\t\traise ValueError(\n\t\t\tf\"Invalid dependency keys. missing={missing}, extra={extra}\"\n\t\t)\n\n\tfor job, prereqs in dependencies.items():\n\t\tunknown = sorted(set(prereqs) - known)\n\t\tif unknown:\n\t\t\traise ValueError(f\"Unknown prerequisites for {job}: {unknown}\")\n\n\t# Kahn's algorithm to reject cycles in scheduling policy.\n\tindegree = {job: 0 for job in job_names}\n\toutgoing: dict[str, list[str]] = {job: [] for job in job_names}\n\tfor job, prereqs in dependencies.items():\n\t\tindegree[job] = len(prereqs)\n\t\tfor prereq in prereqs:\n\t\t\toutgoing[prereq].append(job)\n\n\tqueue = [job for job in job_names if indegree[job] == 0]\n\tseen = 0\n\twhile queue:\n\t\tcurrent = queue.pop()\n\t\tseen += 1\n\t\tfor nxt in outgoing[current]:\n\t\t\tindegree[nxt] -= 1\n\t\t\tif indegree[nxt] == 0:\n\t\t\t\tqueue.append(nxt)\n\n\tif seen != len(job_names):\n\t\traise ValueError(\"Dependency policy contains a cycle\")\n\n\ndef build_manifest_paths(jobs_dir: Path) -> dict[str, Path]:\n\tmanifest_paths: dict[str, Path] = {}\n\tfor job_name in JOB_NAMES:\n\t\tmanifest_path = jobs_dir / f\"part3-{job_name}.yaml\"\n\t\tif not manifest_path.exists():\n\t\t\traise FileNotFoundError(f\"Missing manifest file: {manifest_path}\")\n\t\tmanifest_paths[job_name] = manifest_path\n\treturn manifest_paths\n\n\ndef configure_logging() -> None:\n\tlogging.basicConfig(\n\t\tlevel=logging.INFO,\n\t\tformat=\"%(asctime)s %(levelname)s %(message)s\",\n\t)\n\n\ndef parse_args() -> argparse.Namespace:\n\tparser = argparse.ArgumentParser(\n\t\tdescription=\"Schedule Part 3 PARSEC jobs in Kubernetes with dependencies.\",\n\t)\n\tparser.add_argument(\n\t\t\"--jobs-dir\",\n\t\ttype=Path,\n\t\tdefault=REPO_ROOT / \"automation\" / \"results\" / \"part3\" / \"diego_tentative5\",\n\t\thelp=\"Directory containing parsec-*.yaml job manifests.\",\n\t)\n\tparser.add_argument(\n\t\t\"--output-dir\",\n\t\ttype=Path,\n\t\tdefault=None,\n\t\thelp=\"Optional directory for writing effective (post-edit) job manifests.\",\n\t)\n\tparser.add_argument(\n\t\t\"--namespace\",\n\t\tdefault=\"default\",\n\t\thelp=\"Kubernetes namespace for the jobs.\",\n\t)\n\tparser.add_argument(\n\t\t\"--kube-context\",\n\t\tdefault=None,\n\t\thelp=\"Optional kubeconfig context name.\",\n\t)\n\tparser.add_argument(\n\t\t\"--continue-on-failure\",\n\t\taction=\"store_true\",\n\t\thelp=\"Do not stop scheduling when a job fails.\",\n\t)\n\tparser.add_argument(\n\t\t\"--watch-timeout-seconds\",\n\t\ttype=int,\n\t\tdefault=300,\n\t\thelp=\"Timeout for each Kubernetes watch cycle.\",\n\t)\n\treturn parser.parse_args()\n\nif __name__ == \"__main__\":\n\tsys.exit(main())\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["<<<<<<< SEARCH\n# EVOLVE-BLOCK-START \ndef edit_job_configuration(yaml_objects: list[dict]) -> list[dict]:\n\t\"\"\"\n\tfor job_conf in yaml_objects:\n\t\tcase:\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-blackscholes\"\n\t\t\t\tconfigure as intended blackscholes node, vCPU and memory request/limits, threads, taskset\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-radix\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-vips\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-barnes\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-canneal\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-freqmine\"\n\t\t\t\n\t\t\tswitch job_conf['metadata']['name'] == \"parsec-streamcluster\"\n\t\"\"\"\n\treturn yaml_objects\n=======\n# EVOLVE-BLOCK-START \ndef edit_job_configuration(yaml_objects: list[dict]) -> list[dict]:\n\t# Optimize resource allocation for each job based on their characteristics:\n\t# 1. Radix (short, low resource usage) can run on any core, but must be on node-a due to OOM risk on node-b.\n\t# 2. Barnes and Vips should not share node with memcached (node-a) due to LLC interference.\n\t# 3. Streamcluster is long-running and memory-intensive; isolate it to node-a.\n\t# 4. Blackscholes is safe on node-a and can use 4-8 threads (sub-linear scaling).\n\t# 5. Canneal is LLC-inert but can benefit from 2-4 threads (avoid overloading node-a's LLC).\n\t# 6. Freqmine is CPU-bound and faster on node-b; use 4 threads (max for node-b).\n\n\t# Prioritize resource constraints and SLO compliance:\n\t# - Ensure memcached (on node-a) has dedicated core 0 and 1GB of memory (1024Mi)\n\t# - Streamcluster uses 32GB of node-a's 32GB (remaining 24GB available for other jobs)\n\t# - Barnes and Vips are placed on node-b (no shared LLC with memcached)\n\n\t# Apply configuration changes:\n\t# - Radix: 1 thread, no memory limits (small, quick)\n\t# - Barnes: 1 thread, 1024Mi memory limit (avoid LLC interference)\n\t# - Vips: 1 thread, 1024Mi memory limit (same as Barnes)\n\t# - Blackscholes: 4 threads, 8 vCPU (8000m), 4GB memory (4Gi)\n\t# - Canneal: 2 threads, 2048Mi memory (2Gi) (avoid overloading node-a's LLC)\n\t# - Freqmine: 4 threads, 4 vCPU (4000m), 4GB memory (4Gi) (node-b)\n\t# - Streamcluster: 4 threads, 16GB memory (16Gi), 8 vCPU (8000m) (max for node-a)\n\n\t# Node affinity:\n\t# - Barnes and Vips: nodeSelector: node==node-b\n\t# - All others: node==node-a\n\n\t# Taskset pinning:\n\t# - Radix: core 1 (after memcached's core 0)\n\t# - Streamcluster: cores 1-4 (avoid memcached's core 0)\n\t# - Blackscholes: cores 5-8 (after Streamcluster's cores 1-4)\n\t# - Canneal: cores 5-6 (after Streamcluster's cores 1-4)\n\t# - Freqmine: cores 7-10 (on node-b, using all 4 cores)\n\t# - Barnes and Vips: cores 1-4 (on node-b, shared cores 1-4)\n\n\t# Apply changes to each job configuration:\n\tupdated_yaml_objects = []\n\tfor job_conf in yaml_objects:\n\t\tif job_conf['metadata']['name'] == \"parsec-radix\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-a'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x2']  # core 1\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '100m'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '100m'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['memory'] = '100Mi'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['memory'] = '100Mi'\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telif job_conf['metadata']['name'] == \"parsec-barnes\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-b'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x1']  # core 1 (node-b)\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '1000m'  # 1 vCPU\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '1000m'\n\t\t\tjob_conf['spec']['template']['spec']['containers']['requests']['memory'] = '1024Mi'  # 1Gi\n\t\t\tjob_conf['spec']['template']['spec']['containers']['limits']['memory'] = '1024Mi'\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telif job_conf['metadata']['name'] == \"parsec-vips\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-b'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x1']  # core 1 (node-b)\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '1000m'  # 1 vCPU\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '1000m'\n\t\t\tjob_conf['spec']['template']['spec']['containers']['requests']['memory'] = '1024Mi'  # 1Gi\n\t\t\tjob_conf['spec']['template']['spec']['containers']['limits']['memory'] = '1024Mi'\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telif job_conf['metadata']['name'] == \"parsec-blackscholes\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-a'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x5-0x8']  # cores 5-8\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '8000m'  # 8 vCPU\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '8000m'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['memory'] = '4Gi'  # 4GB\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['memory'] = '4Gi'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'].append('-n', '4')  # 4 threads\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telif job_conf['metadata']['name'] == \"parsec-canneal\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-a'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x5-0x6']  # cores 5-6\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '2000m'  # 2 vCPU\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '2000m'\n\t\t\tjob_conf['spec']['template']['spec']['containers']['requests']['memory'] = '2Gi'  # 2Gi\n\t\t\tjob_conf['spec']['template']['spec']['containers']['limits']['memory'] = '2Gi'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'].append('-n', '2')  # 2 threads\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telif job_conf['metadata']['name'] == \"parsec-freqmine\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-b'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x7-0x10']  # cores 7-10 (all 4 cores on node-b)\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '4000m'  # 4 vCPU\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '4000m'\n\t\t\tjob_conf['spec']['template']['spec']['containers']['requests']['memory'] = '4Gi'  # 4Gi\n\t\t\tjob_conf['spec']['template']['spec']['containers']['limits']['memory'] = '4Gi'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'].append('-n', '4')  # 4 threads\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telif job_conf['metadata']['name'] == \"parsec-streamcluster\":\n\t\t\tjob_conf['spec']['template']['spec']['nodeSelector'] = {'node': 'node-a'}\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'] += ['taskset', '0x1-0x4']  # cores 1-4\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['resources']['requests']['cpu'] = '8000m'  # 8 vCPU\n\t\t\tjob_conf['template']['spec']['containers'][0]['resources']['limits']['cpu'] = '8000m'\n\t\t\tjob_conf['spec']['template']['spec']['containers']['requests']['memory'] = '16Gi'  # 16Gi (max for node-a's 32Gi)\n\t\t\tjob_conf['spec']['template']['spec']['containers']['limits']['memory'] = '16Gi'\n\t\t\tjob_conf['spec']['template']['spec']['containers'][0]['command'].append('-n', '4')  # 4 threads\n\t\t\tupdated_yaml_objects.append(job_conf)\n\t\telse:\n\t\t\tupdated_yaml_objects.append(job_conf)\n\treturn updated_yaml_objects\n>>>>>>> REPLACE\n\n# Scheduling policy (easy to edit): map job -> prerequisite jobs.\n# A job is eligible to be scheduled when all prerequisites are complete.\n# This policy encodes:\n# - streamcluster\n# - freqmine -> barnes -> vips\n# - canneal\n# - blackscholes -> radix\t\nJOB_DEPENDENCIES: dict[str, list[str]] = {\n\t\"parsec-streamcluster\": [],\n\t\"parsec-freqmine\": [],\n\t\"parsec-canneal\": [\"parsec-streamcluster\"],\n\t\"parsec-blackscholes\": [\"parsec-streamcluster\"],\n\t\"parsec-barnes\": [\"parsec-freqmine\"],\n\t\"parsec-vips\": [\"parsec-barnes\", \"parsec-freqmine\"],\n\t\"parsec-radix\": [\"parsec-blackscholes\", \"parsec-streamcluster\"],\n}\n\n# Add a new dependency to ensure streamcluster is scheduled after radix and blackscholes:\n# streamcluster -> radix, blackscholes\n\n# Update the dependency for streamcluster:\nJOB_DEPENDENCIES[\"parsec-streamcluster\"] = [\"parsec-radix\", \"parsec-blackscholes\"]\n\n# EVOLVE-BLOCK-END\n\n# This change ensures that streamcluster (longest job) is scheduled after its dependencies are complete.\n# This reduces makespan by allowing streamcluster to run only when its dependencies (radix and blackscholes) are done,\n# which reduces waiting time and potential interference from other jobs.\n\n# Note: This change also ensures that streamcluster's execution does not overlap with the shorter jobs like radix,\n# which may improve SLO compliance by reducing the total system load during critical periods.\n\n# Further optimization: Use a more efficient scheduling policy to reduce makespan further.\n# Consider using a greedy algorithm to prioritize jobs with shorter runtime first or using a heuristic to minimize dependencies.\n\n# Example: \n# 1. Schedule radix first (shortest job) to reduce initial wait time.\n# 2. Schedule blackscholes and canneal next (both can run concurrently with radix).\n# 3. Schedule streamcluster after radix and blackscholes are done.\n\n# This can be achieved by modifying the `schedule_ready_jobs` function to prioritize jobs based on their runtime or dependencies.\n\n# Example of a greedy scheduling heuristic (to be implemented in `schedule_ready_jobs`):\n# - Sort jobs by their expected runtime (based on known scaling factors)\n# - Schedule the shortest jobs first to reduce makespan\n# - Ensure dependencies are respected\n\n# Example of a dependency-aware scheduling function:\ndef schedule_ready_jobs(\n\tapi_client: client.ApiClient,\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\tmanifest_paths: dict[str, Path],\n\toutput_dir: Path | None,\n\tdependencies: dict[str, list[str]],\n\tunscheduled: set[str],\n\tcompleted: set[str],\n\tfailed: set[str],\n) -> list[str]:\n\t# Sort jobs by their expected runtime (in descending order) to prioritize shorter jobs\n\t# and reduce makespan.\n\t# Use a heuristic to balance dependencies and runtime:\n\t# 1. Prioritize jobs with no dependencies first (radix, canneal)\n\t# 2. Then schedule jobs with dependencies that are already completed\n\t# 3. Use a priority queue to manage the scheduling order\n\tfrom heapq import heappush, heappop\n\n\t# Priority queue: (job, expected_runtime, dependencies)\n\tpq = []\n\tfor job in JOB_NAMES:\n\t\tif job in unscheduled and set(dependencies[job]).issubset(completed):\n\t\t\t# Estimate runtime based on job characteristics (approximate for demonstration)\n\t\t\tif job == \"parsec-radix\":\n\t\t\t\t# Shortest job, 1-2 seconds\n\t\t\t\texpected_runtime = 1.0\n\t\t\telif job == \"parsec-canneal\":\n\t\t\t\t# 2-3 seconds\n\t\t\t\texpected_runtime = 2.5\n\t\t\telif job == \"parsec-blackscholes\":\n\t\t\t\t# 10-15 seconds (sub-linear scaling, assume 12 seconds)\n\t\t\t\texpected_runtime = 12.0\n\t\t\telif job == \"parsec-freqmine\":\n\t\t\t\t# 15-20 seconds (CPU-bound, assume 17 seconds)\n\t\t\t\texpected_runtime = 17.0\n\t\t\telif job == \"parsec-streamcluster\":\n\t\t\t\t# 60-90 seconds (longest, assume 75 seconds)\n\t\t\t\texpected_runtime = 75.0\n\t\t\telif job == \"parsec-barnes\" or job == \"parsec-vips\":\n\t\t\t\t# 5-10 seconds (assuming 7.5 seconds)\n\t\t\t\texpected_runtime = 7.5\n\t\t\telse:\n\t\t\t\traise ValueError(f\"Unknown job: {job}\")\n\t\t\theappush(pq, (expected_runtime, job, dependencies[job]))\n\n\t# Process jobs in priority order (shortest first)\n\twhile pq:\n\t\texpected_runtime, job, prereqs = heappop(pq)\n\t\tif job in unscheduled and set(prereqs).issubset(completed):\n\t\t\tcreate_or_track_existing_job(\n\t\t\t\tapi_client=api_client,\n\t\t\t\tbatch_api=batch_api,\n\t\t\t\tnamespace=namespace,\n\t\t\t\tjob_name=job,\n\t\t\t\tmanifest_path=manifest_paths[job],\n\t\t\t\toutput_dir=output_dir,\n\t\t\t)\n\t\t\tunscheduled.remove(job)\n\t\t\tlogging.info(\"SCHEDULED job=%s deps=%s\", job, prereqs)\n\t\t\tif job == \"parsec-streamcluster\":\n\t\t\t\t# Streamcluster is critical, log its dependency status\n\t\t\t\tlogging.info(\"Streamcluster now scheduled after dependencies completed\")\n\t\telse:\n\t\t\t# If dependencies are not met, skip this job\n\t\t\tcontinue\n\n\t# This heuristic reduces makespan by prioritizing shorter jobs first and ensures dependencies are respected.\n\t# It may need adjustments based on actual runtime data, but it's a good starting point.\n\n# The above change improves makespan by scheduling shorter jobs first and respecting dependencies.\n# It also reduces the chance of interference from longer jobs during critical periods.\n\n# Additional suggestions:\n# - Monitor and adjust resource allocation based on actual job execution times and SLO compliance.\n# - Use more sophisticated scheduling algorithms (e.g., job shop scheduling, constraint programming) if possible.\n# - Implement a feedback loop to adjust resource allocation based on observed performance.\n\n# Example of how to integrate monitoring and feedback:\n# After each job completes, log its actual runtime and adjust the expected_runtime estimates for future scheduling.\n# This can be done by adding a callback in the `watch_for_job_updates` function to update runtime estimates.\n\n# Example of a simple feedback loop:\n# In `watch_for_job_updates`, after a job finishes:\n# 1. Update the expected_runtime for that job in the priority queue\n# 2. Update the dependency graph if necessary (e.g., if a job took longer than expected)\n\n# Example:\ndef watch_for_job_updates(\n\tbatch_api: client.BatchV1Api,\n\tnamespace: str,\n\ttracked_jobs: set[str],\n\tcompleted: set[str"]}}, "artifacts_json": "{\"program_path\": \"/var/folders/gs/p9fl4f1s1yqfl8801x50nhjc0000gn/T/tmphelf3qmx.py\", \"output_dir\": \"/Users/diekgbbtt/cloud-comp-arch-project/openevolve/runs/run_000013\", \"start_time_utc\": \"2026-05-07T21:57:02.439978+00:00\", \"agent_a_ip\": \"35.187.118.71\", \"agent_b_ip\": \"34.140.178.113\", \"measure_ip\": \"34.76.200.0\", \"preclean_mcperf_agent-a_stdout\": \"\", \"preclean_mcperf_agent-a_stderr\": \"Warning: Permanently added '35.187.118.71' (ED25519) to the list of known hosts.\\n\", \"preclean_mcperf_agent-b_stdout\": \"\", \"preclean_mcperf_agent-b_stderr\": \"Warning: Permanently added '34.140.178.113' (ED25519) to the list of known hosts.\\n\", \"preclean_mcperf_measure_stdout\": \"\", \"preclean_mcperf_measure_stderr\": \"Warning: Permanently added '34.76.200.0' (ED25519) to the list of known hosts.\\n\", \"memcached_pod_ip\": \"100.96.5.91\", \"mcperf_warmup_stdout\": \"\", \"mcperf_warmup_stderr\": \"Warning: Permanently added '34.76.200.0' (ED25519) to the list of known hosts.\\n\", \"mcperf_load_stdout_path\": \"/Users/diekgbbtt/cloud-comp-arch-project/openevolve/runs/run_000013/mcperf-measure-load-run13.txt\", \"mcperf_load_stderr_path\": \"/Users/diekgbbtt/cloud-comp-arch-project/openevolve/runs/run_000013/mcperf-measure-load-run13.stderr.txt\", \"scheduler_cmd\": \"/Users/diekgbbtt/cloud-comp-arch-project/automation/.venv/bin/python3.13 /var/folders/gs/p9fl4f1s1yqfl8801x50nhjc0000gn/T/tmphelf3qmx.py --namespace default --jobs-dir /Users/diekgbbtt/cloud-comp-arch-project/automation/results/part3/diego_tentative2 --output-dir /Users/diekgbbtt/cloud-comp-arch-project/openevolve/runs/run_000013\", \"scheduler_stdout\": \"\", \"scheduler_stderr\": \"2026-05-07 23:57:15,019 INFO Loaded kubeconfig context=current\\n2026-05-07 23:57:15,328 INFO SCHEDULED job=parsec-streamcluster deps=[]\\n2026-05-07 23:57:15,392 INFO SCHEDULED job=parsec-freqmine deps=[]\\n2026-05-07 23:59:27,466 INFO FINISHED job=parsec-freqmine status=Succeeded\\n2026-05-07 23:59:27,633 INFO SCHEDULED job=parsec-barnes deps=['parsec-freqmine']\\n2026-05-07 23:59:51,404 INFO FINISHED job=parsec-streamcluster status=Succeeded\\n2026-05-07 23:59:51,467 INFO SCHEDULED job=parsec-canneal deps=['parsec-streamcluster']\\n2026-05-07 23:59:51,526 INFO SCHEDULED job=parsec-blackscholes deps=['parsec-streamcluster']\\n2026-05-08 00:00:13,704 INFO FINISHED job=parsec-barnes status=Succeeded\\n2026-05-08 00:00:13,777 INFO SCHEDULED job=parsec-vips deps=['parsec-barnes', 'parsec-freqmine']\\n2026-05-08 00:01:16,777 INFO FINISHED job=parsec-vips status=Succeeded\\n2026-05-08 00:01:49,750 INFO FINISHED job=parsec-blackscholes status=Succeeded\\n2026-05-08 00:01:49,919 INFO SCHEDULED job=parsec-radix deps=['parsec-blackscholes', 'parsec-streamcluster']\\n2026-05-08 00:02:49,917 INFO FINISHED job=parsec-canneal status=Succeeded\\n2026-05-08 00:03:03,962 INFO FINISHED job=parsec-radix status=Succeeded\\n2026-05-08 00:03:03,963 INFO All jobs finished successfully: ['parsec-barnes', 'parsec-blackscholes', 'parsec-canneal', 'parsec-freqmine', 'parsec-radix', 'parsec-streamcluster', 'parsec-vips']\\n2026-05-08 00:03:03,964 INFO Scheduler finished in 348.95 seconds\\n\", \"job_name_map_json\": \"{\\n  \\\"parsec-barnes\\\": \\\"parsec-barnes\\\",\\n  \\\"parsec-blackscholes\\\": \\\"parsec-blackscholes\\\",\\n  \\\"parsec-canneal\\\": \\\"parsec-canneal\\\",\\n  \\\"parsec-freqmine\\\": \\\"parsec-freqmine\\\",\\n  \\\"parsec-radix\\\": \\\"parsec-radix\\\",\\n  \\\"parsec-streamcluster\\\": \\\"parsec-streamcluster\\\",\\n  \\\"parsec-vips\\\": \\\"parsec-vips\\\"\\n}\", \"pods_json_path\": \"/Users/diekgbbtt/cloud-comp-arch-project/openevolve/runs/run_000013/pods_run_13.json\", \"job_timeline_json\": \"{\\n  \\\"parsec-barnes\\\": {\\n    \\\"duration_s\\\": 43.0,\\n    \\\"end_epoch_s\\\": 1778191211.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-b-4core\\\",\\n    \\\"pod\\\": \\\"parsec-barnes-j68t9\\\",\\n    \\\"start_epoch_s\\\": 1778191168.0\\n  },\\n  \\\"parsec-blackscholes\\\": {\\n    \\\"duration_s\\\": 113.0,\\n    \\\"end_epoch_s\\\": 1778191305.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-a-8core\\\",\\n    \\\"pod\\\": \\\"parsec-blackscholes-fg648\\\",\\n    \\\"start_epoch_s\\\": 1778191192.0\\n  },\\n  \\\"parsec-canneal\\\": {\\n    \\\"duration_s\\\": 174.0,\\n    \\\"end_epoch_s\\\": 1778191366.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-a-8core\\\",\\n    \\\"pod\\\": \\\"parsec-canneal-px7rd\\\",\\n    \\\"start_epoch_s\\\": 1778191192.0\\n  },\\n  \\\"parsec-freqmine\\\": {\\n    \\\"duration_s\\\": 129.0,\\n    \\\"end_epoch_s\\\": 1778191164.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-b-4core\\\",\\n    \\\"pod\\\": \\\"parsec-freqmine-b6frg\\\",\\n    \\\"start_epoch_s\\\": 1778191035.0\\n  },\\n  \\\"parsec-radix\\\": {\\n    \\\"duration_s\\\": 71.0,\\n    \\\"end_epoch_s\\\": 1778191381.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-a-8core\\\",\\n    \\\"pod\\\": \\\"parsec-radix-2xz6t\\\",\\n    \\\"start_epoch_s\\\": 1778191310.0\\n  },\\n  \\\"parsec-streamcluster\\\": {\\n    \\\"duration_s\\\": 153.0,\\n    \\\"end_epoch_s\\\": 1778191188.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-a-8core\\\",\\n    \\\"pod\\\": \\\"parsec-streamcluster-kgt87\\\",\\n    \\\"start_epoch_s\\\": 1778191035.0\\n  },\\n  \\\"parsec-vips\\\": {\\n    \\\"duration_s\\\": 59.0,\\n    \\\"end_epoch_s\\\": 1778191273.0,\\n    \\\"exit_code\\\": 0,\\n    \\\"node\\\": \\\"node-b-4core\\\",\\n    \\\"pod\\\": \\\"parsec-vips-5sgxj\\\",\\n    \\\"start_epoch_s\\\": 1778191214.0\\n  }\\n}\", \"job_effective_configs_json\": \"{\\n  \\\"parsec-barnes\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"./run -a run -S splash2x -p barnes -i native -n 4\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-barnes\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-b-4core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"4\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"3\\\"\\n      }\\n    }\\n  },\\n  \\\"parsec-blackscholes\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"taskset -c 2-3 ./run -a run -S parsec -p blackscholes -i native -n 2\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-blackscholes\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-a-8core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"3\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"2\\\"\\n      }\\n    }\\n  },\\n  \\\"parsec-canneal\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"taskset -c 4-7 ./run -a run -S parsec -p canneal -i native -n 4\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-canneal\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-a-8core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"6\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"4\\\"\\n      }\\n    }\\n  },\\n  \\\"parsec-freqmine\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"./run -a run -S parsec -p freqmine -i native -n 4\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-freqmine\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-b-4core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"4\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"3\\\"\\n      }\\n    }\\n  },\\n  \\\"parsec-radix\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"taskset -c 1 ./run -a run -S splash2x -p radix -i native -n 1\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-radix\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-a-8core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"2\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"1\\\"\\n      }\\n    }\\n  },\\n  \\\"parsec-streamcluster\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"taskset -c 1-7 ./run -a run -S parsec -p streamcluster -i native -n 7\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-streamcluster\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-a-8core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"7\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"6\\\"\\n      }\\n    }\\n  },\\n  \\\"parsec-vips\\\": {\\n    \\\"args\\\": [\\n      \\\"-c\\\",\\n      \\\"./run -a run -S parsec -p vips -i native -n 4\\\"\\n    ],\\n    \\\"command\\\": [\\n      \\\"/bin/sh\\\"\\n    ],\\n    \\\"job_name\\\": \\\"parsec-vips\\\",\\n    \\\"node_selector\\\": {\\n      \\\"cca-project-nodetype\\\": \\\"node-b-4core\\\"\\n    },\\n    \\\"resources\\\": {\\n      \\\"limits\\\": {\\n        \\\"cpu\\\": \\\"4\\\"\\n      },\\n      \\\"requests\\\": {\\n        \\\"cpu\\\": \\\"3\\\"\\n      }\\n    }\\n  }\\n}\", \"mcperf_p95_series_ms_json\": \"[0.43210000000000004, 0.4516, 0.5175, 0.5042, 0.7805, 0.48560000000000003, 0.4762, 0.4586, 0.4616, 0.5071, 0.4347, 0.4632, 0.4809, 0.5054, 1.0735, 0.5699, 0.5344, 0.5671, 0.5519, 0.5624, 0.5135, 0.5245, 0.5042, 0.45389999999999997, 0.5812999999999999, 0.6416000000000001, 0.4839, 0.4724, 0.4838, 0.4811, 0.5044, 0.4797]\"}", "artifact_dir": null, "embedding": null}