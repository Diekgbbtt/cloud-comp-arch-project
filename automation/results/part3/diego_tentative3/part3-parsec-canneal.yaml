apiVersion: batch/v1
kind: Job
metadata:
  name: parsec-canneal
  labels:
    name: parsec-canneal
spec:
  template:
    spec:
      containers:
      - image: anakli/cca:parsec_canneal
        name: parsec-canneal
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh"]
        args: ["-c", "taskset -c 3-7 ./run -a run -S parsec -p canneal -i native -n 5"]
        resources:
          requests:
            cpu: "5"
            # memory: "1Gi"
          limits:
            cpu: "7"
            # memory: "1Gi"
      restartPolicy: Never
      nodeSelector:
        cca-project-nodetype: "node-a-8core"
